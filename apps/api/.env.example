# Copiar a .env y ajustar. Nunca commitear .env con secretos reales.

# Obligatorio en producción: cadena larga y aleatoria (p. ej. openssl rand -base64 48)
# JWT_SECRET=

# Orígenes del front (coma separada). Por defecto localhost Vite.
# CORS_ORIGINS=https://tu-dominio.com,https://www.tu-dominio.com

# Intentos de login fallidos por IP en ventana de 15 min (por defecto 12)
# LOGIN_RATE_MAX_FAILS=12

# NODE_ENV=production
# PORT=3001
